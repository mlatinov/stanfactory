% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tar-bayes-check.R
\name{tar_bayes_check}
\alias{tar_bayes_check}
\title{Check a fitted model on real data (targets factory)}
\usage{
tar_bayes_check(
  spec,
  fit,
  data,
  prior_sensitivity = TRUE,
  thresholds = bayes_thresholds(),
  pars = NULL
)
}
\arguments{
\item{spec}{A \code{\link[=model_spec]{model_spec()}} declaring what the model exposes. Drives which
\code{stanviz} functions are dispatched.}

\item{fit}{A symbol or string naming the upstream fit target (the
\code{CmdStanMCMC} / fit object). E.g. \code{fit = m3_fit} or \code{fit = "m3_fit"}.}

\item{data}{A symbol or string naming the upstream target holding the
observed data list (used to pull the outcome for PPC).}

\item{prior_sensitivity}{Logical. If \code{TRUE} (default) and the spec declares
an \code{lprior}, emit a priorsense power-scaling target. Set \code{FALSE} to skip
during rapid iteration.}

\item{thresholds}{A list from \code{\link[=bayes_thresholds]{bayes_thresholds()}} controlling verdict
scoring.}

\item{pars}{Optional character vector of parameters to focus diagnostics /
trace plots on. \code{NULL} uses the model's default monitored parameters.}
}
\value{
A list of \code{tar_target} objects. Splice it into your \verb{_targets.R}
target list.
}
\description{
Emits a list of \code{targets} targets that interrogate an \strong{already-fitted}
model: convergence diagnostics, posterior predictive checks, out-of-sample
fit (LOO), and optional prior-sensitivity power-scaling. The factory never
fits anything — you own the fit target and its sampler settings, and hand
its name in here. Plotting is delegated to \code{stanviz}; this factory only
orchestrates, scores a verdict, and gathers everything into a single
\code{\link[=bayes_result]{bayes_result()}}.
}
\details{
This is the mode-(a) workhorse: edit a model's Stan code, \code{tar_make()}, and
only this model's check targets re-run. Glance at \code{verdict()}; open the
report only when it is not green.
}
\examples{
\dontrun{
# in _targets.R
list(
  tar_target(m3_data, make_data()),
  tar_target(m3_fit, m3_model$sample(data = m3_data, adapt_delta = 0.99)),
  tar_bayes_check(spec = m3_spec, fit = m3_fit, data = m3_data)
)
}
}

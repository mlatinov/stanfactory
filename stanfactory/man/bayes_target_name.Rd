% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/target-name.R
\name{bayes_target_name}
\alias{bayes_target_name}
\title{Derive a deterministic target name}
\usage{
bayes_target_name(
  kind,
  model_id,
  dataset_id = NULL,
  estimand_id = NULL,
  design_id = NULL,
  suffix = NULL
)
}
\arguments{
\item{kind}{Character scalar. The factory / stage producing the target,
e.g. \code{"fit"}, \code{"check"}, \code{"ppc"}, \code{"summ"}, \code{"prior"}, \code{"recov"},
\code{"stress"}, \code{"compare"}, \code{"report"}.}

\item{model_id}{Character scalar identifying the model. Required.}

\item{dataset_id}{Optional character scalar identifying the dataset
(inference side).}

\item{estimand_id}{Optional character scalar identifying the estimand.}

\item{design_id}{Optional character scalar identifying a design point
(validation side).}

\item{suffix}{Optional trailing tag (e.g. \code{"agg"}, \code{"verdict"}).}
}
\value{
A syntactically valid, unique target name (character scalar).
}
\description{
The single source of truth for how \code{targets} names are constructed across
every factory in stanfactory. Names derive deterministically from the
identity tuple so that (a) editing one model's Stan code invalidates only
that model's downstream targets, and (b) registering a new model variant
fans out predictably. Never paste target-name strings by hand elsewhere;
always route through this function.
}
\examples{
bayes_target_name("fit", model_id = "m3", dataset_id = "d1")
bayes_target_name("summ", model_id = "m3", dataset_id = "d1", estimand_id = "ate")
bayes_target_name("recov", model_id = "m3", design_id = "g07")
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-spec.R
\name{model_spec}
\alias{model_spec}
\title{Declare what a Stan model exposes (the sidecar)}
\usage{
model_spec(
  model_id,
  outcome,
  y_rep = "y_rep",
  log_lik = "log_lik",
  pit = NULL,
  lprior = NULL,
  estimands = character(0),
  arms = NULL,
  groups = NULL,
  data_vars = NULL
)
}
\arguments{
\item{model_id}{Character scalar. Stable identifier for this model. Used as
the spine of every derived target name, so keep it short and unique
(e.g. \code{"m3"}, \code{"hier_v2"}).}

\item{outcome}{Character scalar. Name of the observed outcome in your data
list (e.g. \code{"y"}). Used by PPC plots as the \code{y} argument.}

\item{y_rep}{Character scalar. Generated-quantity name for posterior
predictive draws. Default \code{"y_rep"}. Set \code{NULL} to disable PPC dispatch.}

\item{log_lik}{Character scalar or \code{NULL}. Pointwise log-likelihood name for
LOO. Default \code{"log_lik"}. \code{NULL} disables LOO / out-of-sample dispatch.}

\item{pit}{Character scalar or \code{NULL}. PIT quantity name. \code{NULL} (default)
disables PIT calibration dispatch.}

\item{lprior}{Character scalar or \code{NULL}. Log-prior name enabling priorsense
power-scaling. \code{NULL} (default) disables prior-sensitivity dispatch.}

\item{estimands}{Named character vector mapping short estimand ids to their
generated-quantity names, e.g. \code{c(ate = "est_ate", cate = "est_cate")}.
The names are the ids used in target naming; the values are the Stan
variable names. Empty by default.}

\item{arms}{Named character vector of per-arm prediction quantities, e.g.
\code{c(treated = "mu_treated", control = "mu_control")}. Enables
counterfactual / ATE-from-arms dispatch. \code{NULL} by default.}

\item{groups}{Named list describing hierarchical structure with elements
\code{effects} (group-effect vector name), \code{grand} (grand-mean name), and
\code{sd} (group SD name). \code{NULL} by default; enables the hierarchical suite.}

\item{data_vars}{Optional character vector of expected keys in the data
list. When supplied, factories assert a data list carries exactly these
keys before fitting, catching shape mismatches at build time.}
}
\value{
An object of class \code{model_spec}.
}
\description{
A \code{model_spec} is the interface between your Stan model and the stanfactory
factories. The factories never parse Stan code; they trust this declaration.
It records the generated-quantity names your model emits, so the reporting
layer can dispatch to the appropriate \code{stanviz} functions: a populated
\code{pit} field enables \code{plot_ppc_pit()}, populated \code{arms} enable
\code{extract_ate_from_draws()} + \code{plot_ate_posterior()}, populated \code{groups}
enable the hierarchical suite, and so on.
}
\details{
The same names are what you would otherwise pass to \code{stanviz} at every call
site; declaring them once here removes that repetition and lets the report
template light up sections polymorphically.
}
\examples{
model_spec(
  model_id  = "m3",
  outcome   = "y",
  estimands = c(ate = "est_ate", cate = "est_cate"),
  arms      = c(treated = "mu_treated", control = "mu_control")
)
}

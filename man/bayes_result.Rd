% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes-result.R
\name{bayes_result}
\alias{bayes_result}
\title{The stanfactory result object}
\usage{
bayes_result(
  model_id,
  kind = c("check", "prior", "inference", "stress", "compare"),
  tables = list(),
  plots = list(),
  verdict = NULL,
  meta = list()
)
}
\arguments{
\item{model_id}{Character scalar. The model this result pertains to. For
\code{kind = "compare"} use a composite id (e.g. \code{"m1_m2_m3"}).}

\item{kind}{One of \code{"check"}, \code{"prior"}, \code{"inference"}, \code{"stress"},
\code{"compare"}.}

\item{tables}{Named list of data frames / tibbles (diagnostics, loo,
powerscale, reduced scalar grids, ...). May be empty.}

\item{plots}{Named list of \code{ggplot} objects. May be empty (e.g. the
simulation side stores scalars, not plots).}

\item{verdict}{A one-row data frame: the cheap health glance. Should carry
at least \code{model_id}, \code{kind}, and a \code{status} column
(\code{"green"}/\code{"amber"}/\code{"red"}/\code{"info"}).}

\item{meta}{Named list of free-form metadata (mode, grid dims, timings).}
}
\value{
An object of class \code{bayes_result}.
}
\description{
Every factory emits exactly one \code{bayes_result} as its terminal target. It is
the contract that the reporting layer (\code{tar_bayes_report()}) and the
interactive glance (\code{verdict()}) consume. Crucially, computation and
rendering are separated: the result carries the computed tables, the named
\code{stanviz} plots, and a one-row verdict, so you can pull any plot at the REPL
(\code{res$plots$ppc_dens}) independently of whether a report was ever rendered.
}
